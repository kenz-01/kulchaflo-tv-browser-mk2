{
  "name": "kulchaflo-player-lab",
  "version": "0.1.0-checkpoint3a",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/*.mjs && node --check fixtures/*.mjs && node --check test/*.mjs && npm test",
    "browser:check": "node src/browser-runtime.mjs --check",
    "profile": "node -e \"throw new Error('profile is not implemented in Checkpoint 3A')\"",
    "fixture": "node fixtures/server.mjs",
    "profile:fixture": "node src/profile-fixture.mjs"
  },
  "devDependencies": {
    "playwright": "1.61.1"
  }
}
